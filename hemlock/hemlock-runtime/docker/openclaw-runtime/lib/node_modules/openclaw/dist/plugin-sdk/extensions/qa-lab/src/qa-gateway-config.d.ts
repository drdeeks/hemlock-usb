import type { OpenClawConfig } from "openclaw/plugin-sdk/config-runtime";
import type { ModelProviderConfig } from "openclaw/plugin-sdk/provider-model-shared";
import { type QaProviderMode } from "./model-selection.js";
export declare const DEFAULT_QA_CONTROL_UI_ALLOWED_ORIGINS: readonly string[];
export type QaThinkingLevel = "off" | "minimal" | "low" | "medium" | "high" | "xhigh" | "adaptive";
export declare function normalizeQaThinkingLevel(input: unknown): QaThinkingLevel | undefined;
export declare function mergeQaControlUiAllowedOrigins(extraOrigins?: string[]): string[];
export declare function buildQaGatewayConfig(params: {
    bind: "loopback" | "lan";
    gatewayPort: number;
    gatewayToken: string;
    providerBaseUrl?: string;
    qaBusBaseUrl: string;
    includeQaChannel?: boolean;
    workspaceDir: string;
    controlUiRoot?: string;
    controlUiAllowedOrigins?: string[];
    controlUiEnabled?: boolean;
    providerMode?: QaProviderMode | "live-openai";
    primaryModel?: string;
    alternateModel?: string;
    imageGenerationModel?: string | null;
    enabledProviderIds?: string[];
    enabledPluginIds?: string[];
    liveProviderConfigs?: Record<string, ModelProviderConfig>;
    fastMode?: boolean;
    thinkingDefault?: QaThinkingLevel;
}): OpenClawConfig;
