import type { ClawdbotConfig, RuntimeEnv } from "../runtime-api.js";
import { createFeishuClient } from "./client.js";
import { type CommentFileType } from "./comment-target.js";
type FeishuCommentReactionClient = ReturnType<typeof createFeishuClient> & {
    request(params: {
        method: "POST";
        url: string;
        data: unknown;
        timeout: number;
    }): Promise<unknown>;
};
export declare function cleanupAmbientCommentTypingReaction(params: {
    client: FeishuCommentReactionClient;
    deliveryContext?: {
        channel?: string;
        to?: string;
        threadId?: string | number;
    };
    runtime?: RuntimeEnv;
}): Promise<boolean>;
export declare function createCommentTypingReactionLifecycle(params: {
    cfg: ClawdbotConfig;
    fileToken: string;
    fileType: CommentFileType;
    replyId?: string;
    accountId?: string;
    runtime?: RuntimeEnv;
}): {
    start: () => Promise<void>;
    cleanup: () => Promise<void>;
};
export {};
