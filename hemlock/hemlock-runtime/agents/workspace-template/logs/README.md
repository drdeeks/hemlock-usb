# logs/ — per-agent activity logs

Agent-level logging lands here (session transcripts, tool runs, memory-search logs
for verifiability). Rotated/archived by the owner, never silently discarded.
Populated at runtime; empty in the template.
