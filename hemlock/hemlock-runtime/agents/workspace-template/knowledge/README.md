# knowledge/ — the agent's link into the global knowledge base

Per-agent knowledge staging (tools/knowledge.sh). Captured knowledge flows to the
runtime-root append-only knowledge base with classified use/function/scope links.
Append-only by design: knowledge is added and linked, never removed.
